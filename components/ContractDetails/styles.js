import { StyleSheet } from "react-native";

export const styles = StyleSheet .create({
    container: {
        width:"90%",
        paddingTop:10,
        paddingHorizontal:20,
        margin:10
    },
    gap: {
      marginBottom: 12,
    }
  });