import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

   
    listContainer: {
        flexDirection: 'column',
        alignItems: 'center',
        paddingVertical: 10,
    },
    title:{
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom:10
      }
    
});