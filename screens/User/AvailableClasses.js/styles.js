import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

   
    listContainer: {
        flexDirection: 'column',
        alignItems: 'center'
    },
    title:{
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom:10
      }
    
});